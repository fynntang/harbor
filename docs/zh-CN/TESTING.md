# 测试与证据

[English](../TESTING.md) | 简体中文 · [文档目录](README.md)

## 本轮文档核查：2026-09-08

重写和翻译文档前已阅读实现。本轮只修改文档，以下检查是在当前本地工作区重新执行的，不由旧记录推断：

| 检查 | 结果 |
|---|---|
| `cargo metadata --locked --no-deps --format-version 1` | 包为 `harbor-cli` / `harbor-core`，Cargo 版本 `0.0.1`，CLI 二进制目标仍为 `harbor`。 |
| `sh scripts/check.sh` | 退出 0：格式检查、74 项 Rust 测试、拒绝警告的 Clippy、release 构建。 |
| Rust 测试组成 | 4 项 JSON + 13 项 CLI 根目录保护 + 40 项核心单元 + 11 项 Profile + 6 项版本测试，零失败。 |
| `swift test --package-path apps/Harbor --scratch-path target/swift-harbor` | 12 项通过，零失败。 |
| 文档校验 | 12 页 / 6 组语言对应页；122 处本地链接/锚点；32 个代码块；双语技术标识及 13 个 CLI 命令已核对。 |
| 本机工具链 | rustc/Cargo `1.97.1`、Apple Swift `6.3.2`、macOS arm64。 |

Rust 和原生 Swift 测试使用临时夹具和子进程，包括临时废纸篓/恢复操作，不需要真实账号文件。进程检查、签名/原生测试需要相应 macOS 工具和权限。本次在本机受限执行沙箱外运行。

文档校验覆盖本地链接/锚点、对应语言页面、命令代码块一致性及实际 CLI help。本轮仅整理文档，没有重新验收远程 CI、外部参考 URL、真实客户端创建、真实账号登录、GUI 点击或 Dock 显示。该次核查发现 GUI/Cargo 版本分别维护；后续版本统一检查见下文。

## 复现检查

在仓库根目录执行：

```bash
sh scripts/check.sh
swift test --package-path apps/Harbor --scratch-path target/swift-harbor
./scripts/build_and_run.sh --verify
cargo run --locked -p harbor-cli -- --help
```

Rust 脚本检查格式但不改写文件，测试/Clippy/release 使用 `--locked`，失败立即退出。Cargo.lock 应与源码一起保留。`--verify` 还会重新构建/启动本 checkout 的 Harbor GUI，只检查进程存活，不验证可见交互。[CI](../../.github/workflows/ci.yml) 在 macOS 和 Ubuntu 运行 Rust 脚本，在 macOS 执行 Swift 测试和打包；本文不宣称远程 CI 已通过。平台测试有条件编译，Linux 数量不必等于 macOS。

## 覆盖与限制

| 范围 | 自动化覆盖 |
|---|---|
| 存储 | 名称、路径重叠/默认账号保护、别名/大小写、不覆盖登记、锁释放、符号链接日志与日志尾部。 |
| 环境/启动 | 允许名单/保留名称、精确命令识别、独立会话/标准流、早退、短版本/构建号门禁及清单不变。 |
| 复制 | 空 Profile、冲突、准备失败、排他发布竞态、权限过滤、拒绝未签名来源。 |
| 图标 | 合成 PNG 校验、资源尺寸、本地签名、数据/元数据不变、运行中主进程拒绝与不安全资源结构。 |
| 生命周期 | 保留数据删除、明确删除数据、回滚、拒绝运行/接管/改路径数据、已知 helper 路径、保留活动子进程及退出孤儿进程。 |
| Swift | JSON/退出码/协议校验、字面 argv、双管道读取、状态流程/重试、退出拒绝阻止删除、图像转换、错误原生身份与废纸篓回滚。 |

单元/模拟测试不证明所有应用能力。夹具使用生成的 plist/脚本或复制的系统程序，不打包厂商应用和凭据。源码检查还发现文本状态仅查主进程、JSON 状态包含辅助进程，以及 CLI/GUI 图标保护不同；已记录在[架构说明](ARCHITECTURE.md)。

## 历史本机实验：2026-09-08

下表汇总此前开发记录。它们属于历史观察，不是在最终文档工作区上的复跑、发行认证，也不代表当前已安装实例的状态。

| 实验 | 当时记录的结果与边界 |
|---|---|
| 锁回归 | 继承/复制的句柄曾延迟锁释放，加入 drop 显式解锁和回归测试。当时的 15 项核心测试连续通过 100 轮，共 1,500 次执行；不是当前 74 项测试连续跑 100 轮。 |
| 从原版创建 | 来源 `/Applications/ChatGPT.app`，版本 `26.901.51231`、构建 `8109`。临时副本通过来源/复制/本地签名检查，数据为空，显示登录页且拒绝重复启动；原版/已有副本的主程序和 plist 哈希不变。未测试账号登录或 OAuth。 |
| 迁入持久目录 | 经用户明确授权，将临时 `work` 迁入持久目录，codex/gui inode 保持不变，修正应用路由并检查签名。这是一次性维护操作，不是已提供的迁移命令。 |
| 实例图标 | 真实临时副本接受 Dock/菜单栏资源，签名/doctor 通过，ASAR 和清单不变、数据仍为空；该图标实验未启动客户端。另执行合成图标测试。 |
| 停止/删除 | 真实临时副本暴露了登记时序和孤儿 helper 问题，随后修复。最终两次启动后立即停止成功，运行时删除被拒绝，默认保留合成数据，明确数据删除/废纸篓恢复检查通过；测试文件已清理。自动删除测试未指向真实账号。 |
| GUI 交互 | Computer Use 多次返回 `native pipe closed`；之后部分读取获取了删除表单/错误及最终空列表。没有确立完整点击/关闭/重开验收。当时空列表不代表当前 registry。 |
| Harbor 图标 | 检查过十种 ICNS 尺寸、资源一致性、签名和 GUI 启动。蓝色底板后来改为透明白色线条帆船，透明度/线条颜色检查通过，未直接验证最终 Dock 缓存显示。 |
| 包名/脚本整理 | CLI 包/目录改为 `harbor-cli`、二进制仍为 `harbor`；脚本统一到 `scripts` 并同步 Run/CI/docs 引用，当时相关构建/检查通过。旧 `harbor 0.1.0` 输出属于历史，不是当前 Cargo 版本。 |

`SOURCE_CHECKS.txt` 保留初始源码生成快照，包括旧包名与 NOT RUN 条目。此前 Python 启动和用户提供的 plist/窗口观察用于选择适配器，不是当前 Rust 实现的验收结果。

## 仍需手工验收

破坏性/恢复检查应使用明确可丢弃的 Profile，真实登录由用户执行。生产账号数据不进入自动化夹具。

| 操作 | 观察内容 |
|---|---|
| 全新 clone | 新 bundle 和空数据，原版及既有 Profile 不受影响。 |
| 首次启动与重复启动 | 正确登录窗口，重复启动不新增实例。 |
| 关闭终端 / Harbor | 客户端保持独立运行。 |
| 停止或取消退出 | 正确客户端退出，或拒绝/超时保留 Profile 并阻止删除。 |
| 保留/删除数据 | 范围符合确认，明确的保留/废纸篓路径可恢复。 |
| 图标及窗口/菜单生命周期 | Dock/Finder/菜单显示、重启/缓存、关闭重开和 busy 控件。 |
| 多账号重启 | 各实例恢复正确账号和对话。 |
| OAuth / Browser / Computer Use | 实例路由正确，功能实际可用。 |
| Skills / MCP / 工具 | 记录共享/实例来源、PATH、SSH 和 Docker 行为。 |
| 版本变化 | 审查警告、启动拦截、单次例外及数据库兼容性。 |

不要公开完整客户端日志或把真实凭据复制进测试。`doctor` 不检查所有配置覆盖或账号身份，版本/冲突警告也可能不使其失败。接管 Profile 的回退可继续使用已验证的旧启动器和原路径；接管本身未迁移数据库或重签应用。不要把删除数据库、Singleton 文件或注册锁当作恢复捷径。删除后的恢复遵循[当前流程](GUI.md#removal-and-recovery)。

## 版本统一：2026-09-08

CLI、核心包和 GUI 版本统一使用 `0.0.1`；打包从 Cargo metadata 读取 CLI 版本，不再维护另一份硬编码版本。macOS 构建号仍为 `1`。已移除应用结构错误提示及注释中的旧版本文字。

格式/shell 语法检查、11 项 Profile 测试和 6 项版本测试通过。打包、本地签名验证和 GUI 启动通过；实际 app 的 Info.plist、内置 `harbor --version` 与 Cargo metadata 已对照，均为 `0.0.1`。本次仅统一版本，未重跑完整 Rust/Swift 测试集。

## GUI 语言切换（2026-09-08）

- 完整 Swift 测试：**16 项通过**，其中新增 4 项本地化测试覆盖默认语言解析、偏好保存、已有提示即时更新、插值参数保留及底层诊断原文不变。
- 文案审计：114 处中文 GUI 文案均有英文翻译；进程状态、进度状态及翻译参数数量均已覆盖。
- `Harbor.app` 打包、签名验证和进程启动通过。界面检查覆盖 English/简体中文即时切换、英文创建表单、表单输入保留，以及重新构建/启动后保留英文选择。检查期间未创建或删除客户端实例。
- Rust 逻辑未改动；上文 Rust 结果属于此前验证记录。本次 GUI 翻译范围不包括 CLI/底层诊断及系统控制的文字。
- 严格 Swift lint 仍报告 `Profile.swift` 中 11 个既有 snake_case JSON 字段命名；本次保留这些协议模型名称，不宣称完整 lint 通过。

## 发布构建准备（2026-09-08）

- 提交 `edef48f` 的 GitHub Actions 在 macOS 和 Ubuntu 上均通过。
- 本地 Swift GUI 和原生辅助程序的 release 编译通过；开发包的构建、签名验证和启动检查仍通过。
- 5 项 Python 发布门槛测试通过；真实 ad-hoc 开发包被发布验证器拒绝。Shell 语法、双语文档及本地链接检查通过（12 页、124 处链接/锚点、34 个代码块）。
- 本机钥匙串中没有可用的 Developer ID Application 签名身份。真实 Developer ID 签名、安全时间戳、公证、另一台 Mac 的安装验收和 Release 发布仍未验证。本次仅完成签名配置入口和验证门槛。
